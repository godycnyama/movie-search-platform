# Secrets & local env — NEVER commit
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# .NET
bin/
obj/
*.user

# Pipeline artifacts / logs
pipeline/logs/
*.log

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Terraform (state + local backend config)
*.tfstate
*.tfstate.*
backend.hcl
.terraform.lock.hcl
# Local backend override files — must never reach CI/CD (it uses the S3 backend).
*_override.tf
